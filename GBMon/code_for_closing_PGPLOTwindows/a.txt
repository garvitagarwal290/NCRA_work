char* out= (char*) malloc(5);
string pid = "";

FILE* fp=popen("ps -e|grep pgxwin_server","r");
fgets(out,10,fp);

for (int i = 0; i < 6; i++) {
pid = pid + out[i];
}

stringstream kill;
kill<<"kill "<<pid;
system(kill.str().c_str());
